import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.com.domain.tourismtracker',
  appName: 'tourismtracker',
  webDir: 'www'
};

export default config;
