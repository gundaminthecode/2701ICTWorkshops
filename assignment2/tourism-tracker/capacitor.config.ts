import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.com.domain.tourismtracker',
  appName: 'Tourism Tracker',
  webDir: 'www'
};

export default config;
